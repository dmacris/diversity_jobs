# For the custom setup on running things with the bundle in a custom location we will need those
# So we do not get "tired"
alias rails='bundle exec rails'
alias rspec='RAILS_ENV=test bundle exec rspec'
alias rubocop='bundle exec rubocop'
